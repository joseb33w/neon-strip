# Godot
.godot/
/out/
export_presets.cfg.bak

# Downloaded toolchain (never commit the engine/templates)
godot
*.tpz
*.tmp

# Verification / preview tooling (never committed)
/verify/
/playbooks/

# Binary game assets — fetched/generated by the scripts in scripts/ (see README "Building").
# (The bulk of the world + all Meshy hero pieces stream from R2 at runtime.)
/models/*.glb
/models/Textures/
/audio/*.wav
/audio/*.ogg
*.import
